# Vean http://help.github.com/ignore-files/ para aprender como ignorar archivos.
# o bien, vean https://www.w3schools.com/git/git_ignore.asp?remote=github

# compiled output (salida compilada)
/dist
/tmp
/out-tsc

# Runtime data (datos de ejecucion)
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover (directorio para librerias instrumentadas por jsciverage/JScover)
lib-cov

# Coverage directory used by tools like istanbul (Directorio de cobertura usado por herramientas como istanbul)
coverage

# nyc test coverage (cobertura de tests nyc
.nyc_output

# Grunt intermediate storage (almacenamiento intermedio de grunt) [http://gruntjs.com/creating-plugins#storing-task-files]
.grunt

# Bower dependency directory (directorio de dependencias de Bower) [https://bower.io/]
bower_components

# node-waf configuration (configuracion de nodos waf)
.lock-wscript

# IDEs and editors (archivos de los IDE's y editores)
.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode (archivos especificos de VS code)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc (archivos miscelaneos)
.sass-cache
connect.lock
typings

# Logs (archivos de LOGS)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*


# Dependency directories (directorios de dependencias)
node_modules/
jspm_packages/

# Optional npm cache directory (directorio de cache npm opcional)
.npm

# Optional eslint cache (cache eslint opcional)
.eslintcache

# Optional REPL history (historial de REPL opcional)
.node_repl_history

# Output of 'npm pack' (salida del pack npm)
*.tgz

# Yarn Integrity file (archivo de integridad Yarn)
.yarn-integrity

# dotenv environment variables file (archivo de bariables de entorno dotenv)
.env

# next.js build output (salida del compilado next.js)
.next

# Lerna
lerna-debug.log

# System Files (archivos de sistema)
.DS_Store
Thumbs.db